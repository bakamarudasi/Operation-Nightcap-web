# プロジェクト振り返り・反省点

Operation Nightcap (CLOSURE'S BAR) の開発で得た教訓をまとめる。
次回のプロジェクトで同じ失敗を繰り返さないためのドキュメント。

---

## 1. 設計・アーキテクチャ

### God Component 問題
- `BattleScreen.tsx`（814行）、`GachaScreen.tsx`（938行）が巨大すぎる
- バトル描画・ロジック・アニメーション・音声・ダイアログ全部が1ファイル
- **教訓**: 1コンポーネント200行を超えたら分割を検討する

### モノリシックStore
- `gameStore.ts`（1036行）に全状態を詰め込んだ
- ゲーム状態・バトルロジック・デッキ管理・ショップ・ガチャ・永続化が1ファイル
- `playRound()` だけで300行超のネスト地獄
- **教訓**: Zustandでも機能ごとにstoreを分割する（`useBattleStore`, `useDeckStore` 等）

### バトルエンジン肥大化
- `battleEngine.ts`（1526行）に計算・バフ・カード効果・マッチアップ全部
- **教訓**: ドメインロジックは関心ごとに分離する（`buffSystem.ts`, `damageCalc.ts` 等）

---

## 2. 多言語対応（i18n）

### 後付けi18nの代償
- 日本語ハードコードで開発 → 後から35ファイル変更、500+翻訳キー作成
- キャラ台詞・カードデータに日本語直書きしていたため全部書き直し
- **教訓**: テキストはデータファイルに直書きしない。最初から定数 or 翻訳キーで管理

### 翻訳ファイルが巨大
- `ja.ts` / `en.ts` 各1099行の単一ファイル
- **教訓**: namespaceで機能ごとに分割する（`common.json`, `battle.json`, `cards.json`）

### キーの命名規則が統一されていない
- カード: `cards.${id}.name`
- バフ: `buff.${id}`
- キャラ: `char.blaze.cg.${key}.dialogue`
- パターンがバラバラで覚えにくい
- **教訓**: 翻訳キーの命名規則を最初にドキュメント化する

### ハードコード日本語が残っている
- `BattleScreen.tsx` に `'🔥 発情状態のカードを使った…自分に酔い+1！'` が残存
- `gameStore.ts` の console.log に日本語
- **教訓**: ESLintカスタムルールでハードコード日本語を検出する仕組みを入れる

---

## 3. データ設計

### カード効果の定義が二重システム
- 古いカード: `damage`, `drunkDamage`, `applyBuffs` 等の個別フィールド
- 新しいカード: `effects` 配列の宣言的システム
- 2つのシステムが混在していて混乱の元
- **教訓**: データスキーマは最初に統一する。途中で変えるなら全マイグレーションする

### データとプレゼンテーションの混在
- `characters.ts` にテーマカラー・ポートレートパス・翻訳キーが全部入り
- **教訓**: 構造データ（色・パス）と振る舞いデータ（台詞・バトルライン）は分離する

### マジックナンバーの散在
- バトル参加費 `500`、酔いレベル閾値、アニメーション時間 `600ms` 等がコード直書き
- **教訓**: `constants.ts` に集約する

---

## 4. 品質基盤の欠如

### テストゼロ
- テストファイルが一切存在しない
- バトルロジック（1526行）、カード強化、デッキ管理が未テスト
- **教訓**: 複雑なドメインロジック（バトルエンジン等）は最初からテストを書く

### Lint/Formatter未導入
- ESLint、Prettierの設定なし
- コードスタイルの統一が人力頼み
- **教訓**: プロジェクト初日に `eslint` + `prettier` を入れる

### CI/CDなし
- GitHub Actionsなし。ビルド検証・型チェックが自動化されていない
- **教訓**: 最低限 `tsc --noEmit` と `vite build` をCIで回す

### エラーバウンダリなし
- Reactコンポーネントにエラーバウンダリがない
- 1コンポーネントのエラーでアプリ全体がクラッシュする
- **教訓**: 画面単位でエラーバウンダリを入れる

---

## 5. パフォーマンス

### 再レンダリング問題
- BattleScreenが39個のstore selectorを個別に呼んでいる
- Zustandのセレクタ未使用で、store更新のたびに全コンポーネントが再レンダリング
- **教訓**: `useShallow` やカスタムセレクタでメモ化する

### メモ化の欠如
- `Card` コンポーネントがループ内で `React.memo` なしで描画
- `buildCardCountMap()` が毎レンダリング再計算
- **教訓**: リスト内の子コンポーネントは `React.memo` で包む

### コード分割なし
- 全画面が初回ロードで一括読み込み
- **教訓**: `React.lazy` + `Suspense` で画面単位の遅延読み込みを入れる

---

## 6. コンポーネント設計

### 命名規則の不統一
- `CGOverlay`, `AfterEventOverlay`（Overlay系）
- `BattleScreen`, `ShopScreen`（Screen系）
- `BuffDisplay`（Display系）
- **教訓**: 命名規則を決めてドキュメント化する

### 重複コード
- `gameStore.ts` でプレイヤー/相手のcleanse処理が2回コピペ
- corrupted slots のロジックも重複
- **教訓**: プレイヤー/相手で共通化できる処理は関数に切り出す

---

## 7. 次回プロジェクトでやること（チェックリスト）

- [ ] プロジェクト初日に ESLint + Prettier + Husky を導入
- [ ] テキストは最初から翻訳キー or 定数ファイルで管理
- [ ] Zustand storeは機能ごとに分割（1store 200行以内）
- [ ] コンポーネントは200行超えたら分割を検討
- [ ] マジックナンバーは `constants.ts` に集約
- [ ] GitHub Actionsで型チェック・ビルド・テストを自動化
- [ ] データスキーマを統一してから実装を始める
- [ ] 翻訳キーの命名規則をドキュメント化
- [ ] エラーバウンダリを画面単位で設置
- [ ] React.memo / useMemo / useCallback を適切に使う
